export type CreateAccessFormData = {
    origin: string;
    name: string;
    email: string;
    password: string;
    phone: string;
    code?: string;
    fantasyName?: string,
    assignment?: string,
}