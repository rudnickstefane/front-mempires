export type InitialConfigureResponse = {
    initialConfigure: {
        id: string;
        profileCode: string;
        companyCode: number;
        status: string;
        createdAt: string;
        updatedAt: string;
    };
}